---
phase: 04-quote-review-and-history
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/components/quotes/status-badge.tsx
  - apps/mobile/src/components/quotes/quote-row.tsx
  - apps/mobile/src/components/quotes/line-item-row.tsx
  - apps/mobile/src/components/quotes/price-edit-sheet.tsx
  - apps/mobile/src/components/quotes/catalog-picker-sheet.tsx
  - apps/mobile/src/components/quotes/quote-detail.tsx
autonomous: true
requirements:
  - REVIEW-01
  - REVIEW-02
  - REVIEW-03
  - REVIEW-08
  - HIST-03

must_haves:
  truths:
    - "StatusBadge renders all 7 quote statuses with correct colors and labels"
    - "QuoteRow displays phone, status badge, total price, and relative date"
    - "LineItemRow shows item name, quantity stepper, and tappable price"
    - "PriceEditSheet opens with current price and saves on confirm"
    - "CatalogPickerSheet lists active catalog items for selection"
    - "QuoteDetail renders read-only line items with status and total"
  artifacts:
    - path: "apps/mobile/src/components/quotes/status-badge.tsx"
      provides: "Colored pill badge for 7 quote statuses"
      exports: ["StatusBadge"]
    - path: "apps/mobile/src/components/quotes/quote-row.tsx"
      provides: "History list row component"
      exports: ["QuoteRow"]
    - path: "apps/mobile/src/components/quotes/line-item-row.tsx"
      provides: "Editable line item with stepper and swipe-to-delete"
      exports: ["LineItemRow"]
    - path: "apps/mobile/src/components/quotes/price-edit-sheet.tsx"
      provides: "Bottom sheet for editing line item price"
      exports: ["PriceEditSheet"]
    - path: "apps/mobile/src/components/quotes/catalog-picker-sheet.tsx"
      provides: "Bottom sheet for picking catalog items to add"
      exports: ["CatalogPickerSheet"]
    - path: "apps/mobile/src/components/quotes/quote-detail.tsx"
      provides: "Read-only quote detail view"
      exports: ["QuoteDetail"]
  key_links:
    - from: "apps/mobile/src/components/quotes/quote-row.tsx"
      to: "apps/mobile/src/components/quotes/status-badge.tsx"
      via: "import StatusBadge"
      pattern: "import.*StatusBadge.*from.*status-badge"
    - from: "apps/mobile/src/components/quotes/line-item-row.tsx"
      to: "react-native-gesture-handler"
      via: "Swipeable import"
      pattern: "import.*Swipeable.*from.*react-native-gesture-handler"
---

<objective>
Build all reusable UI components for the quotes feature.

Purpose: Create the component library that the screens (Plan 03) will compose. Each component is self-contained with its own styles, props interface, and accessibility attributes. Building components first prevents the screen files from becoming bloated.
Output: 6 components in `src/components/quotes/` ready for screen integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-quote-review-and-history/04-CONTEXT.md
@.planning/phases/04-quote-review-and-history/04-UI-SPEC.md

<interfaces>
<!-- Existing components to replicate patterns from -->

From apps/mobile/src/components/catalog/catalog-row.tsx (Swipeable pattern):
```typescript
// Swipeable wrapper with renderRightActions for delete action
import { Swipeable } from 'react-native-gesture-handler';
function renderRightActions(): JSX.Element {
  return (
    <Pressable style={styles.deleteAction} onPress={onDelete}
      accessibilityRole="button" accessibilityLabel={`Remove ${item.name}`}>
      <Ionicons name="trash-outline" size={24} color="#ffffff" />
    </Pressable>
  );
}
<Swipeable renderRightActions={renderRightActions} overshootRight={false}>
```

From apps/mobile/src/components/catalog/item-form-sheet.tsx (Modal pattern):
```typescript
// Modal with animationType="slide", transparent, overlay + sheet layout
// Overlay: backgroundColor: 'rgba(0,0,0,0.5)', justifyContent: 'flex-end'
// Sheet: borderTopLeftRadius: 16, borderTopRightRadius: 16, maxHeight: screenHeight * 0.8
// Drag handle: width: 32, height: 4, backgroundColor: colors.border, borderRadius: 2
// KeyboardAvoidingView with behavior: Platform.OS === 'ios' ? 'padding' : 'height'
```

From apps/mobile/src/theme/tokens.ts:
```typescript
export const spacing = { xs: 4, sm: 8, md: 16, lg: 24, xl: 32, '2xl': 48, '3xl': 64 } as const;
export const colors = {
  dominant: '#ffffff', secondary: '#f5f5f5', accent: '#0066cc',
  destructive: '#dc2626', destructivePressed: '#b91c1c', warning: '#b45309',
  mutedText: '#666666', border: '#cccccc', borderFocused: '#0066cc', errorText: '#dc2626',
  success: '#16a34a',  // added in Plan 01
} as const;
export const typography = {
  body: { fontSize: 16, fontWeight: '400' as const, lineHeight: 24 },
  label: { fontSize: 14, fontWeight: '400' as const, lineHeight: 20 },
  heading: { fontSize: 22, fontWeight: '700' as const, lineHeight: 28 },
  display: { fontSize: 20, fontWeight: '700' as const, lineHeight: 26 },  // added in Plan 01
} as const;
export const MIN_TOUCH_TARGET = 44;
```

From apps/mobile/src/db/models/catalog-item.ts:
```typescript
export class CatalogItem extends Model {
  static table = 'catalog_items';
  @text('name') name!: string;
  @field('unit_price_cents') unitPriceCents!: number;
  @text('trade_category') tradeCategory!: string | null;
  @field('is_archived') isArchived!: boolean;
}
```

LineItem type (from Plan 01 — apps/mobile/src/utils/line-items.ts):
```typescript
export interface LineItem {
  catalogItemId: string;
  name: string;
  quantity: number;
  unitPriceCents: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatusBadge + QuoteRow + QuoteDetail components</name>
  <files>
    apps/mobile/src/components/quotes/status-badge.tsx,
    apps/mobile/src/components/quotes/quote-row.tsx,
    apps/mobile/src/components/quotes/quote-detail.tsx
  </files>
  <read_first>
    apps/mobile/src/theme/tokens.ts,
    apps/mobile/src/components/catalog/catalog-row.tsx,
    apps/mobile/src/components/catalog/empty-state.tsx,
    apps/mobile/src/db/models/quote.ts,
    .planning/phases/04-quote-review-and-history/04-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/mobile/src/components/quotes/status-badge.tsx`:**

    Props: `{ status: string }`. Self-contained color mapping. No external props for colors.

    Status-to-display mapping:
    - `draft_local` -> label: "Draft", bg: `#e0f0ff`, text: `#0066cc`
    - `draft_queued` -> label: "Queued", bg: `#e0f0ff`, text: `#0066cc`
    - `sent` -> label: "Sent", bg: `#f0f0f0`, text: `#666666`
    - `approved` -> label: "Approved", bg: `#dcfce7`, text: `#16a34a`
    - `declined` -> label: "Declined", bg: `#fee2e2`, text: `#dc2626`
    - `expired` -> label: "Expired", bg: `#fee2e2`, text: `#dc2626`
    - `failed_send` -> label: "Failed", bg: `#fee2e2`, text: `#dc2626`

    Use a `Record<string, { label: string; bg: string; text: string }>` const map.

    Style: `borderRadius: 4`, `paddingHorizontal: spacing.sm` (8), `paddingVertical: spacing.xs` (4), text `fontSize: 14`, `fontWeight: '700'`.

    **2. Create `apps/mobile/src/components/quotes/quote-row.tsx`:**

    Props: `{ quote: Quote; onPress: (quote: Quote) => void }` where Quote is the WatermelonDB model.

    Layout (horizontal row, Pressable):
    - Left column (flex: 1): customer phone (body 16px/400, fallback "No phone" in mutedText if null/empty), relative date below (label 14px/400, mutedText color). Use `formatRelativeDate` from `src/utils/format-relative-date.ts`.
    - Center: `<StatusBadge status={quote.status} />`
    - Right: total formatted as dollars (`$${(quote.totalCents / 100).toFixed(2)}`), display typography (20px/700).

    Min height: 56px per UI-SPEC. Horizontal padding: `spacing.md` (16). Vertical padding: `spacing.md` (16). Bottom border: 1px `colors.border`.

    `accessibilityRole="button"` on Pressable. `accessibilityLabel` includes status and total.

    **3. Create `apps/mobile/src/components/quotes/quote-detail.tsx`:**

    Props: `{ quote: { status: string; customerPhone: string | null; totalCents: number; createdAt: string; sentAt: string | null }; lineItems: Array<{ id: string; name: string; quantity: number; unitPriceCents: number }> }`.

    Read-only view:
    - Header area: StatusBadge (large, top), customer phone, date info
    - FlatList of line items: each row shows name (left, flex 1), "x{quantity}" (center), formatted price (right). No stepper, no swipe — purely read-only.
    - Footer: total formatted as dollars, display typography.

    No interactive elements. No FAB. Used by the read-only `quote/[id].tsx` screen.

    All styles via `StyleSheet.create()`. Use `colors`, `spacing`, `typography` from tokens.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/quotes/status-badge.tsx contains "export function StatusBadge" or "export const StatusBadge"
    - apps/mobile/src/components/quotes/status-badge.tsx contains "draft_local"
    - apps/mobile/src/components/quotes/status-badge.tsx contains "approved"
    - apps/mobile/src/components/quotes/status-badge.tsx contains "#e0f0ff"
    - apps/mobile/src/components/quotes/status-badge.tsx contains "#dcfce7"
    - apps/mobile/src/components/quotes/status-badge.tsx contains "#fee2e2"
    - apps/mobile/src/components/quotes/status-badge.tsx contains "borderRadius: 4"
    - apps/mobile/src/components/quotes/quote-row.tsx contains "export function QuoteRow" or "export const QuoteRow"
    - apps/mobile/src/components/quotes/quote-row.tsx contains "formatRelativeDate"
    - apps/mobile/src/components/quotes/quote-row.tsx contains "StatusBadge"
    - apps/mobile/src/components/quotes/quote-row.tsx contains "toFixed(2)"
    - apps/mobile/src/components/quotes/quote-detail.tsx contains "export function QuoteDetail" or "export const QuoteDetail"
    - apps/mobile/src/components/quotes/quote-detail.tsx contains "FlatList"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>StatusBadge renders all 7 statuses with correct colors/labels, QuoteRow displays phone + badge + total + relative date, QuoteDetail renders read-only line items list with total</done>
</task>

<task type="auto">
  <name>Task 2: LineItemRow + PriceEditSheet + CatalogPickerSheet components</name>
  <files>
    apps/mobile/src/components/quotes/line-item-row.tsx,
    apps/mobile/src/components/quotes/price-edit-sheet.tsx,
    apps/mobile/src/components/quotes/catalog-picker-sheet.tsx
  </files>
  <read_first>
    apps/mobile/src/components/catalog/catalog-row.tsx,
    apps/mobile/src/components/catalog/item-form-sheet.tsx,
    apps/mobile/src/theme/tokens.ts,
    apps/mobile/src/db/models/catalog-item.ts,
    .planning/phases/04-quote-review-and-history/04-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/mobile/src/components/quotes/line-item-row.tsx`:**

    Props:
    ```typescript
    interface LineItemRowProps {
      name: string;
      quantity: number;
      unitPriceCents: number;
      onQuantityChange: (delta: number) => void;
      onPricePress: () => void;
      onDelete: () => void;
    }
    ```

    Layout: Swipeable wrapper (copy pattern from `catalog-row.tsx`).

    Inner row (horizontal):
    - Left (flex: 1): item name, body typography (16px/400). Ellipsize if long (`numberOfLines={1}`).
    - Center: +/- stepper. Three elements in a row:
      - `-` button: Pressable, min 44x44 (`MIN_TOUCH_TARGET`), centered Ionicons `remove-circle-outline` size 28, color `colors.accent`. Disabled (opacity 0.4) when `quantity === 1`. `accessibilityLabel="Decrease quantity"`.
      - Quantity text: 20px wide min, body text centered, display the number.
      - `+` button: Pressable, min 44x44, centered Ionicons `add-circle-outline` size 28, color `colors.accent`. `accessibilityLabel="Increase quantity"`.
    - Right: price Pressable. Min height 44px. Text: `$${(unitPriceCents / 100).toFixed(2)}`, display typography (20px/700). `accessibilityLabel="Edit price"`.

    Right swipe action (renderRightActions): Red delete button, background `colors.destructive`, white trash icon (same as catalog-row pattern). `accessibilityLabel={`Remove ${name}`}`.

    Min row height: 56px. Padding: `spacing.md` horizontal, `spacing.sm` vertical. Bottom border: 1px `colors.border`.

    `overshootRight={false}` on Swipeable.

    **2. Create `apps/mobile/src/components/quotes/price-edit-sheet.tsx`:**

    Props:
    ```typescript
    interface PriceEditSheetProps {
      visible: boolean;
      currentPriceCents: number;
      onSave: (newPriceCents: number) => void;
      onDismiss: () => void;
    }
    ```

    Use `Modal` with `animationType="slide"` and `transparent={true}` (same as `item-form-sheet.tsx` pattern).

    Layout:
    - Overlay: `backgroundColor: 'rgba(0,0,0,0.5)'`, `flex: 1`, `justifyContent: 'flex-end'`.
    - Sheet: white bg, `borderTopLeftRadius: 16`, `borderTopRightRadius: 16`, padding `spacing.lg` (24).
    - Drag handle: `width: 32`, `height: 4`, `backgroundColor: colors.border`, `borderRadius: 2`, centered, `marginBottom: spacing.md`.
    - Title: "Edit Price", heading typography (22px/700).
    - TextInput: `keyboardType="decimal-pad"`, pre-filled with current price as dollars (`(currentPriceCents / 100).toFixed(2)`). Border: 1px `colors.border`, focused border: `colors.borderFocused`. Padding: `spacing.md`. Font: body typography.
    - Two buttons side by side:
      - "Discard" (left): text-only, mutedText color, 48px height, `accessibilityLabel="Discard price change"`.
      - "Update Price" (right): accent background, white text, 48px height, `accessibilityLabel="Save new price"`.

    On "Update Price": parse input string to cents (strip `$`, parse float, multiply by 100, round). Call `onSave(newCents)`. Validate > 0; show inline error (errorText color) "Price must be greater than $0.00" if invalid.

    Wrap content in `KeyboardAvoidingView` with `behavior={Platform.OS === 'ios' ? 'padding' : 'height'}`.

    **3. Create `apps/mobile/src/components/quotes/catalog-picker-sheet.tsx`:**

    Props:
    ```typescript
    interface CatalogPickerSheetProps {
      visible: boolean;
      items: Array<{ id: string; name: string; unitPriceCents: number }>;
      onSelect: (item: { id: string; name: string; unitPriceCents: number }) => void;
      onDismiss: () => void;
    }
    ```

    Same Modal pattern as PriceEditSheet.

    Layout:
    - Title: "Add Item", heading typography.
    - FlatList of catalog items. Each row: Pressable, min height 44px, padding `spacing.md`.
      - Left: item name, body typography.
      - Right: price formatted as dollars, label typography, mutedText color.
    - Row separator: 1px `colors.border`.
    - Empty state: "No catalog items available" text if `items.length === 0`.

    On row press: call `onSelect(item)`, then `onDismiss()`.

    No search bar — flat scrollable list is sufficient per CONTEXT.md Claude's Discretion (10-30 items).

    `maxHeight: Dimensions.get('window').height * 0.6` for the sheet.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "export function LineItemRow" or "export const LineItemRow"
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "Swipeable"
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "MIN_TOUCH_TARGET" or "minHeight: 44" or "minWidth: 44"
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "remove-circle-outline"
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "add-circle-outline"
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "opacity" (disabled state for minus at qty=1)
    - apps/mobile/src/components/quotes/line-item-row.tsx contains "onDelete"
    - apps/mobile/src/components/quotes/price-edit-sheet.tsx contains "export function PriceEditSheet" or "export const PriceEditSheet"
    - apps/mobile/src/components/quotes/price-edit-sheet.tsx contains "Modal"
    - apps/mobile/src/components/quotes/price-edit-sheet.tsx contains "Update Price"
    - apps/mobile/src/components/quotes/price-edit-sheet.tsx contains "Discard"
    - apps/mobile/src/components/quotes/price-edit-sheet.tsx contains "KeyboardAvoidingView"
    - apps/mobile/src/components/quotes/price-edit-sheet.tsx contains "decimal-pad"
    - apps/mobile/src/components/quotes/catalog-picker-sheet.tsx contains "export function CatalogPickerSheet" or "export const CatalogPickerSheet"
    - apps/mobile/src/components/quotes/catalog-picker-sheet.tsx contains "Modal"
    - apps/mobile/src/components/quotes/catalog-picker-sheet.tsx contains "FlatList"
    - apps/mobile/src/components/quotes/catalog-picker-sheet.tsx contains "onSelect"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>LineItemRow renders name + stepper + price with swipe-to-delete and 44px touch targets, PriceEditSheet opens with pre-filled price and validates on save, CatalogPickerSheet lists catalog items in a scrollable modal</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` in apps/mobile exits 0
- All 6 component files exist in `src/components/quotes/`
- Every interactive element meets MIN_TOUCH_TARGET=44
- StatusBadge covers all 7 status values with distinct colors
</verification>

<success_criteria>
- 6 components created and type-check clean
- StatusBadge maps all 7 statuses to correct colors and short labels
- LineItemRow has functional +/- stepper with 44px targets and swipe-to-delete
- PriceEditSheet uses Modal pattern with dollar-to-cents conversion
- CatalogPickerSheet lists items with dismiss-on-select
- QuoteDetail renders read-only view suitable for sent/approved/declined quotes
</success_criteria>

<output>
After completion, create `.planning/phases/04-quote-review-and-history/04-02-SUMMARY.md`
</output>
